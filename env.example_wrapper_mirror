# =========================
# Odin-style Mirror Bot ENV
# =========================

# -------- Shredstream proxy --------
export SHREDSTREAM_PROXY_GRPC_URLS="grpc://127.0.0.1:50051"

# -------- Leaders (comma-separated) --------
export LEADER_WALLETS="4EsYuWFZAt1PfNJq8Jr7monip43gNqrQ7k2Kne1npqJx,2sT2usN51Zdoqt6wvTHFa8AySdLAi2rsGybsHwegVnX9,HRV512AYkkxhHpcERBN2x8Xf2mGksPFavwfAr22XSrGb,BNnN2MqfWLvgThYBsv6v8JQaYZXYKYahC5YCy27Ct1cX"
export STRICT_SIGNER=false

# -------- RPC (minimal reads: ALT prefetch + occasional fallbacks) --------
# Chainstack example: https://solana-mainnet.core.chainstack.com/<API_KEY>
export RPC_HTTP_URL="https://solana-mainnet.core.chainstack.com/<API_KEY>"

# Blockhash refresher: 0 = disabled (we use leader tx blockhash when available)
export BLOCKHASH_REFRESH_MS=0

# ALT resolve
export RESOLVE_ALTS=true
export ALT_MAX_CONCURRENT_FETCH=2
# If ALT table missing in cache, skip tx immediately and prefetch ALT in background
export ALT_MISS_SKIP=true

# Parsing/execution
export PARSE_CONCURRENCY=128
export EXECUTOR_CONCURRENCY=8

# Dedup
export DEDUP_SIG_TTL_MS=2000
export DEDUP_MINT_TTL_SECS=5

# -------- Wrapper/router allowlist --------
# Add GMGN/Trojan program IDs you observe in logs.
# Axiom is included automatically.
export WRAPPER_PROGRAM_IDS="<GMGN_PROGRAM_ID>,<TROJAN_PROGRAM_ID>"

# -------- Trade --------
export EXECUTION_MODE=bundle_pumpbuy
export BUY_SOL=0.1
export SLIPPAGE_BPS=500

# Compute budget
export COMPUTE_UNIT_LIMIT=600000
export COMPUTE_UNIT_PRICE_MICRO_LAMPORTS=0

# -------- WSOL bank (recommended) --------
# If WSOL balance < WSOL_MIN_SOL at startup, top-up to WSOL_TARGET_SOL.
export WSOL_MIN_SOL=0.1
export WSOL_TARGET_SOL=0.3
# (optional legacy) fixed wrap once
export WSOL_RESERVE_SOL=0

# -------- Jito --------
export USE_JITO=true
export JITO_BLOCK_ENGINE_URL="https://amsterdam.mainnet.block-engine.jito.wtf"
export JITO_TIP_ACCOUNT="96gYZGLnJYVFmbjzopPSU6QiEV5fGqZNyN9nmNhvrZU5"
export JITO_TIP_LAMPORTS=30000
# Free endpoint throttle (ms)
export JITO_MIN_SUBMIT_INTERVAL_MS=1100

# -------- Wallet --------
export KEYPAIR_PATH="$HOME/.config/solana/id.json"

# -------- Logging --------
export STATS_INTERVAL_SECS=5
export CPI_HEURISTIC_MINT=true
export RUST_LOG=info
